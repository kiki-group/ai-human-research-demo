[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: send all routes to index.html so HashRouter (or any
# future browser router) always loads the app shell.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
